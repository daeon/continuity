interface:
  display_name: "Continuity"
  short_description: "Compact durable memory for resumable agent work"
  default_prompt: "Keep a compact Continuity record so this work can resume correctly across context or agent boundaries."

policy:
  allow_implicit_invocation: true
